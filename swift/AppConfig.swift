import Foundation

enum AppConfig {
    static let shotAnalysisURL = URL(string: "https://www.doach.app")!
    static let apiBaseURL = URL(string: "https://api.doach.app")!
    static let registrationPath = "auth/register"
    static let loginURL = URL(string: "https://www.doach.app/login")!
}

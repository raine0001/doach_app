# ── Secrets
.env
.env.*.local

# ── Python
__pycache__/
*.py[cod]
.venv/
venv/

# ── Node
node_modules/
dist/
build/
npm-debug.log*
yarn-*.log*
pnpm-debug.log*

# ── OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# ── Test & reports (never commit)
playwright-report/
test-results/

# ── Runtime / training artifacts
uploads/
static/videos/       # user uploads
frame_cache/
frames/
datasets/
labels/
runs/
logs/
videos/
data/
artifacts/
sessions/
tests/
tools/

# ── Big binaries (block by default; use Git LFS if you really need them)
*.zip
*.webm
*.mp4

# ── CI (uncomment if you don’t want workflows in the repo)
# .github/workflows/

# ── Project-specific (verify with team)
playwright.config.js
playwright.config.ts
prepare_gpt_yolo_dataset.py
train.py
skipped_frames.txt
skipped_frames.json
PA_app.py
ACCEPTANCE_CRITERIA.md
